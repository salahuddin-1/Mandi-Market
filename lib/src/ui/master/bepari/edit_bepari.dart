import 'package:flutter/material.dart';

class EditBepari extends StatelessWidget {
  const EditBepari({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
