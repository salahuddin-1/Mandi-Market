import 'package:flutter/material.dart';

class AddBepari extends StatelessWidget {
  const AddBepari({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
